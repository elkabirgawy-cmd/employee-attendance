# تقرير مراجعة إعدادات النظام - System Settings Validation Report

**تاريخ المراجعة:** 2026-01-12
**الهدف:** التحقق من أن كل إعداد في صفحة Settings له تأثير واضح ومباشر في النظام

---

## ملخص تنفيذي

تم مراجعة جميع الإعدادات في صفحة Settings والتحقق من استخدامها الفعلي في:
- Frontend (EmployeeApp, Reports, Calculations)
- Backend (Edge Functions)
- Database (Tables & Migrations)

### النتائج الرئيسية:

| القسم | الحالة | ملاحظات |
|-------|--------|---------|
| إعدادات حساب الحضور | ✅ متصل ويعمل | يُستخدم في حساب أيام العمل |
| إعدادات الانصراف التلقائي | ✅ متصل ويعمل | يُستخدم في EmployeeApp |
| إعدادات GPS | ⚠️ UI فقط | غير متصل بقاعدة بيانات |
| قواعد الحضور | ⚠️ UI فقط | غير متصل بقاعدة بيانات |
| الأمان وكشف الاحتيال | ⚠️ UI فقط | غير متصل بقاعدة بيانات |
| الإشعارات | ⚠️ UI فقط | غير متصل بقاعدة بيانات |
| الإعدادات الإقليمية | ⚠️ UI فقط | غير متصل بقاعدة بيانات |

---

## التفاصيل الكاملة

## 1️⃣ إعدادات حساب الحضور ✅

### الإعدادات المتاحة:
- **عدد أيام الشهر**: حساب تلقائي (28-31 يوم)
- **الإجازة الأسبوعية الثابتة**: اختيار الأيام (أحد، إثنين، ... جمعة، سبت)
- **صيغة الحساب**: عرض توضيحي للمعادلة

### الاتصال بقاعدة البيانات:
✅ **متصل**
- الجدول: `attendance_calculation_settings`
- الحقول: `weekly_off_days` (array of integers)
- Migration: `20260111220308_update_to_calendar_based_attendance.sql`

### الاستخدام الفعلي:
✅ **يُستخدم في:**

#### 1. حساب أيام العمل الفعلية
**الملف:** `src/utils/attendanceCalculations.ts`
```typescript
function computeEffectiveWorkdays(
  settings: AttendanceSettings | null,
  employee: EmployeeWorkdaysConfig | null,
  year: number,
  month: number,
  approvedVacationDays: number
): number {
  const weeklyOffDays = employee?.weekly_off_days || settings?.weekly_off_days || [];

  // Count occurrences of weekly off days in the month
  for (const weekday of weeklyOffDays) {
    weeklyOffCount += countWeekdayOccurrences(year, month, weekday);
  }

  // Calculate effective work days
  const effectiveDays = baseDays - weeklyOffCount - approvedVacationDays;
}
```

#### 2. حساب متوسط ساعات العمل اليومية
**الملف:** `src/utils/attendanceCalculations.ts`
```typescript
function calculateMonthlyStats(...): MonthlyStats {
  const effectiveDays = computeEffectiveWorkdays(settings, ...);
  const averageHoursPerDay = totalHours / effectiveDays;
}
```

#### 3. عرض الإحصائيات في شاشة الموظف
**الملف:** `src/pages/EmployeeApp.tsx`
- يتم عرض "متوسط ساعات العمل اليومية" بناءً على أيام العمل الفعلية

### الحكم النهائي:
✅ **VALID** - الإعداد يعمل بشكل صحيح ويؤثر مباشرة على:
- حساب أيام العمل الفعلية في الشهر
- حساب متوسط ساعات العمل اليومية
- عرض الإحصائيات للموظفين
- التقارير الشهرية

---

## 2️⃣ إعدادات الانصراف التلقائي ✅

### الإعدادات المتاحة:
- **تفعيل الانصراف التلقائي** (toggle)
- **مدة العد التنازلي** (autoCheckoutAfterSeconds: 60-3600 ثانية)
- **فترة المراقبة** (watchIntervalSeconds: 5-60 ثانية)
- **عدد القراءات للتأكد** (verifyOutsideWithNReadings: 1-10 قراءات)
- **الحد الأقصى لدقة الموقع** (maxLocationAccuracyMeters: 20-200 متر)

### الاتصال بقاعدة البيانات:
✅ **متصل**
- الجدول: `auto_checkout_settings`
- Migration: `20260112015420_update_auto_checkout_settings_simplified.sql`

### الاستخدام الفعلي:
✅ **يُستخدم في:**

#### 1. شاشة الموظف - مراقبة الموقع
**الملف:** `src/pages/EmployeeApp.tsx` (السطور 1177-1194)
```typescript
// Load settings
const { data } = await supabase
  .from('auto_checkout_settings')
  .select('*')
  .limit(1)
  .maybeSingle();

setAutoCheckoutSettings({
  auto_checkout_enabled: data.auto_checkout_enabled,
  auto_checkout_after_seconds: data.auto_checkout_after_seconds,
  verify_outside_with_n_readings: data.verify_outside_with_n_readings,
  watch_interval_seconds: data.watch_interval_seconds,
  max_location_accuracy_meters: data.max_location_accuracy_meters
});
```

#### 2. بدء العد التنازلي للانصراف التلقائي
**الملف:** `src/pages/EmployeeApp.tsx` (السطور 1200-1230)
```typescript
const startAutoCheckout = (reason: 'OUT_OF_BRANCH' | 'LOCATION_DISABLED') => {
  const startedAt = getServerNowMs();
  const endsAt = startedAt + (autoCheckoutSettings.auto_checkout_after_seconds * 1000);

  setAutoCheckout({
    active: true,
    reason,
    startedAtServerMs: startedAt,
    endsAtServerMs: endsAt,
  });
}
```

#### 3. مراقبة الحالة وتفعيل الانصراف التلقائي
**الملف:** `src/pages/EmployeeApp.tsx` (السطور 942-956)
```typescript
useEffect(() => {
  if (!currentLog || !autoCheckoutSettings || !autoCheckoutSettings.auto_checkout_enabled) {
    if (autoCheckout.active) {
      cancelAutoCheckout();
    }
    return;
  }

  const warningStatus = hasLocationWarning();

  if (warningStatus.hasWarning && !autoCheckout.active && warningStatus.reason) {
    startAutoCheckout(warningStatus.reason);
  } else if (!warningStatus.hasWarning && autoCheckout.active) {
    cancelAutoCheckout();
  }
}, [currentLog, autoCheckoutSettings, locationHealth, isConfirmedOutside, ...]);
```

### الحكم النهائي:
✅ **VALID** - الإعداد يعمل بشكل صحيح ويؤثر مباشرة على:
- تفعيل/تعطيل الانصراف التلقائي
- مدة العد التنازلي المرئي للموظف
- فترة مراقبة الموقع
- عدد القراءات المطلوبة للتأكد من الخروج
- استبعاد القراءات منخفضة الدقة

### ملاحظة مهمة:
⚠️ **Edge Function غير متزامن**
- الملف: `supabase/functions/auto-checkout-enforcement/index.ts`
- يستخدم بنية قديمة مختلفة تماماً:
  - `auto_checkout_on_location_disabled_enabled`
  - `location_disabled_grace_minutes`
  - `auto_checkout_on_no_signal_enabled`
  - `no_signal_grace_minutes`

**التوصية:** Edge Function يحتاج إلى تحديث أو حذف لأنه لا يتطابق مع البنية الحالية.

---

## 3️⃣ إعدادات GPS ⚠️

### الإعدادات المتاحة في UI:
- **أقصى دقة GPS** (defaultValue: 50 متر)
- **عتبة التحذير** (defaultValue: 30 متر)
- **طلب وضع الدقة العالية** (checkbox, defaultChecked)
- **كشف GPS الوهمي** (checkbox, defaultChecked)

### الاتصال بقاعدة البيانات:
❌ **غير متصل**
- لا يوجد جدول `gps_settings` في قاعدة البيانات
- الإعدادات تستخدم `defaultValue` فقط
- لا توجد دالة حفظ (`Save` button غير موجود)

### الاستخدام الفعلي:
❌ **غير مستخدم**
- Edge functions لا تستخدم هذه الإعدادات
- Employee check-in لا يفحص هذه القيم
- مجرد واجهة مستخدم ثابتة

### الحكم النهائي:
❌ **INVALID - UI PLACEHOLDER ONLY**
- الإعدادات لا تؤثر على أي منطق في النظام
- لا يتم حفظها في قاعدة البيانات
- لا يتم استخدامها في Edge Functions
- مجرد عناصر UI زينة بدون وظيفة

### التوصية:
**Option 1: إزالة كاملة**
- حذف القسم بالكامل من Settings.tsx

**Option 2: تنفيذ كامل**
- إنشاء جدول `gps_settings`
- إضافة دالة حفظ
- استخدام القيم في `employee-check-in` edge function
- فحص GPS accuracy قبل قبول check-in

---

## 4️⃣ قواعد الحضور ⚠️

### الإعدادات المتاحة في UI:
- **فترة السماح** (grace period: 15 دقيقة)
- **السماح بالدخول المبكر** (early check-in: 30 دقيقة)
- **طلب تسجيل الخروج** (require checkout: checkbox)
- **حظر تسجيلات الدخول المتكررة** (block duplicate: checkbox)

### الاتصال بقاعدة البيانات:
❌ **غير متصل**
- لا يوجد جدول `attendance_rules` في قاعدة البيانات
- الإعدادات تستخدم `defaultValue` فقط
- لا توجد دالة حفظ (`Save` button غير موجود)

### الاستخدام الفعلي:
❌ **غير مستخدم**
- Edge function `employee-check-in` لا يفحص:
  - Grace period
  - Early check-in allowed
  - Duplicate check-ins
- لا توجد validation لـ require checkout

### الحكم النهائي:
❌ **INVALID - UI PLACEHOLDER ONLY**

### التوصية:
**Option 1: إزالة كاملة**

**Option 2: تنفيذ كامل**
- إنشاء جدول `attendance_rules_settings`
- استخدام grace period في تحديد حالة late/on_time
- منع check-in أكثر من المسموح
- تطبيق early check-in restrictions

---

## 5️⃣ الأمان وكشف الاحتيال ⚠️

### الإعدادات المتاحة في UI:
- **كشف الأجهزة المكسورة الحماية** (checkbox)
- **كشف تطبيقات GPS الوهمية** (checkbox)
- **كشف التلاعب بالوقت** (checkbox)
- **حظر الأجهزة المشبوهة** (checkbox)
- **أقصى قفزة مسافة** (max distance jump: 1000 متر)

### الاتصال بقاعدة البيانات:
❌ **غير متصل**

### الاستخدام الفعلي:
❌ **غير مستخدم**
- لا توجد فحوصات في Edge Functions
- لا يتم تسجيل fraud alerts
- جدول `fraud_alerts` موجود لكن لا يتم ملؤه

### الحكم النهائي:
❌ **INVALID - UI PLACEHOLDER ONLY**

### التوصية:
**Option 1: إزالة كاملة**

**Option 2: تنفيذ كامل**
- إنشاء جدول `security_settings`
- تنفيذ فحوصات في Edge Functions
- استخدام `fraud_alerts` table
- منع check-in من أجهزة مشبوهة

---

## 6️⃣ الإشعارات ⚠️

### الإعدادات المتاحة في UI:
- **إشعار عند التأخير** (checkbox)
- **إشعار عند المغادرة المبكرة** (checkbox)
- **إشعار عند الغياب** (checkbox)
- **إشعار عند تنبيهات الاحتيال** (checkbox)
- **البريد الإلكتروني للإدارة** (email input)

### الاتصال بقاعدة البيانات:
❌ **غير متصل**

### الاستخدام الفعلي:
❌ **غير مستخدم**
- لا توجد edge function لإرسال emails
- لا يتم استخدام email في أي مكان

### الحكم النهائي:
❌ **INVALID - UI PLACEHOLDER ONLY**

### التوصية:
**Option 1: إزالة كاملة**

**Option 2: تنفيذ كامل**
- إنشاء جدول `notification_settings`
- إنشاء edge function لإرسال emails
- ربطها بأحداث النظام (late arrival, early leave, etc.)

---

## 7️⃣ الإعدادات الإقليمية ⚠️

### الإعدادات المتاحة في UI:
- **اللغة الافتراضية** (Arabic/English)
- **المنطقة الزمنية** (Asia/Riyadh, Asia/Dubai, UTC)
- **تنسيق التاريخ** (DD/MM/YYYY, MM/DD/YYYY, YYYY-MM-DD)

### الاتصال بقاعدة البيانات:
❌ **غير متصل**

### الاستخدام الفعلي:
⚠️ **جزئياً**
- اللغة: يُستخدم `LanguageContext` (لكن ليس من Settings)
- المنطقة الزمنية: غير مستخدم (كل التواريخ UTC)
- تنسيق التاريخ: غير مستخدم

### الحكم النهائي:
❌ **INVALID - UI PLACEHOLDER ONLY**

### التوصية:
**Option 1: إزالة من Settings**
- LanguageContext يعمل بشكل مستقل
- لا حاجة لإعدادات إضافية

**Option 2: تنفيذ كامل**
- إنشاء جدول `regional_settings`
- استخدام timezone في التقارير
- استخدام date format في العرض

---

## 📊 الخلاصة والتوصيات

### الإعدادات العاملة (2/7):
✅ **إعدادات حساب الحضور** - يعمل بشكل كامل
✅ **إعدادات الانصراف التلقائي** - يعمل بشكل كامل

### الإعدادات الوهمية (5/7):
❌ **إعدادات GPS** - UI فقط
❌ **قواعد الحضور** - UI فقط
❌ **الأمان وكشف الاحتيال** - UI فقط
❌ **الإشعارات** - UI فقط
❌ **الإعدادات الإقليمية** - UI فقط

---

## التوصيات النهائية

### 🔴 عاجل - Action Required

#### 1. إزالة الإعدادات الوهمية
**السبب:** تضليل المستخدم بإعدادات لا تعمل

**الإجراء:**
```typescript
// حذف هذه الأقسام من Settings.tsx:
- إعدادات GPS (السطور 273-310)
- قواعد الحضور (السطور 312-349)
- الأمان وكشف الاحتيال (السطور 351-386)
- الإشعارات (السطور 549-584)
- الإعدادات الإقليمية (السطور 586-624)
```

#### 2. تحديث/حذف Edge Function
**الملف:** `supabase/functions/auto-checkout-enforcement/index.ts`

**المشكلة:** يستخدم بنية قديمة لا تتطابق مع `auto_checkout_settings` الحالي

**الخيارات:**
- **Option A:** حذفه (الانصراف التلقائي يعمل من client-side)
- **Option B:** تحديثه ليستخدم البنية الجديدة

---

## ملخص الوضع الحالي

| الإعداد | Database | Frontend Use | Backend Use | Status |
|---------|----------|--------------|-------------|--------|
| weekly_off_days | ✅ | ✅ | ❌ | ✅ Working |
| auto_checkout_settings | ✅ | ✅ | ⚠️ Old | ✅ Working |
| GPS settings | ❌ | ❌ | ❌ | ❌ Fake |
| Attendance rules | ❌ | ❌ | ❌ | ❌ Fake |
| Security/Fraud | ❌ | ❌ | ❌ | ❌ Fake |
| Notifications | ❌ | ❌ | ❌ | ❌ Fake |
| Regional | ❌ | ⚠️ Partial | ❌ | ❌ Fake |

---

**تاريخ التقرير:** 2026-01-12
**الحالة:** مراجعة كاملة
**التوصية:** إزالة الإعدادات الوهمية فوراً لتجنب التضليل
