# دليل الاختبار التلقائي لنظام المرتبات (Payroll Simulation)

## 🧪 نظرة عامة

تم إضافة نظام اختبار تلقائي (Simulation) لنظام المرتبات يعمل فقط في **وضع التطوير (Dev Mode)**.

### الأهداف الرئيسية:
1. ✅ **التحقق من دقة حساب النطاق الزمني**: التأكد أن الأيام خارج النطاق المحدد لا تُحتسب كغياب
2. ✅ **اختبار فلترة الجزاءات والمكافآت**: التأكد أن العناصر خارج النطاق يتم تجاهلها
3. ✅ **توفير بيانات تجريبية**: عرض كشف رواتب تجريبي بدون التأثير على قاعدة البيانات
4. ✅ **تسهيل اختبار الميزات**: اختبار سريع للميزات الجديدة بدون إنشاء بيانات حقيقية

---

## 🚀 كيفية التشغيل

### تشغيل تلقائي (Automatic)

عند فتح صفحة "إدارة الرواتب والجزاءات" في وضع التطوير:

```bash
npm run dev
```

1. افتح المتصفح وانتقل إلى صفحة الرواتب
2. سيتم تشغيل الاختبار التلقائي **مرة واحدة فقط** عند أول فتح للصفحة
3. ستظهر رسالة نجاح باللون الأخضر في الأعلى
4. ستظهر لوحة الاختبار باللون البرتقالي تحتوي على:
   - 🧪 عنوان "وضع الاختبار التلقائي"
   - تقرير مفصل بالبيانات التجريبية والحسابات
   - زر لإغلاق الاختبار

### إعادة التشغيل

لإعادة تشغيل الاختبار:

**الطريقة 1:** اضغط زر "إغلاق الاختبار والعودة للبيانات الحقيقية" ثم أعد تحميل الصفحة (F5)

**الطريقة 2:** افتح Console المتصفح واكتب:
```javascript
sessionStorage.removeItem('payroll_simulation_run');
location.reload();
```

**الطريقة 3:** أعد تشغيل dev server:
```bash
# Ctrl+C لإيقاف السيرفر
npm run dev
```

---

## 📊 البيانات التجريبية

### المعلومات الأساسية:

```
الفترة المحددة: يناير 2026 (من يوم 1 إلى يوم 10)
عدد الموظفين: 2
أيام العمل الشهرية: 26 يوم
أيام العمل المتوقعة في النطاق: ~8 أيام
```

---

### موظف 1: أحمد محمد (SIM001)

#### البيانات المالية:
- **الراتب الأساسي:** 5,000 جنيه
- **البدلات:** 500 جنيه
- **نوع الراتب:** شهري

#### سجلات الحضور:

**داخل النطاق (1-10):**
- ✅ يوم 2: حضر (بدون تأخير)
- ✅ يوم 5: حضر (تأخر 15 دقيقة)
- ✅ يوم 8: حضر (بدون تأخير)

**خارج النطاق (يجب تجاهلها):**
- ⚠️ يوم 15: حضر ← **تم تجاهله**

**النتيجة:**
```
أيام الحضور الفعلية: 3 أيام (فقط الأيام داخل النطاق)
أيام الغياب: ~5 أيام (8 متوقع - 3 حضور)
أيام تم تجاهلها: 1 يوم (يوم 15)
```

#### الجزاءات:

**داخل النطاق:**
- ✅ يوم 3: جزاء اختبار 100 جنيه ← **تم تطبيقه**

**خارج النطاق:**
- ⚠️ يوم 13: جزاء اختبار 200 جنيه ← **تم تجاهله**

#### المكافآت:

**داخل النطاق:**
- ✅ يوم 6: مكافأة اختبار 150 جنيه ← **تم تطبيقها**

**النتيجة المتوقعة:**
```
المستحقات:
- الراتب الأساسي: 5,000
- البدلات: 500
- المكافآت: +150
- الإجمالي: 5,650

الخصومات:
- خصم الغياب: ~962 (5 أيام × 192.31 راتب يومي)
- خصم التأخير: ~14.42 (15 دقيقة)
- الجزاءات: 100
- التأمين (10%): 500
- الضرائب (5%): 250
- الإجمالي: ~1,826.42

صافي الراتب: ~3,823.58
```

---

### موظف 2: فاطمة علي (SIM002)

#### البيانات المالية:
- **الراتب الأساسي:** 6,000 جنيه
- **البدلات:** 600 جنيه
- **نوع الراتب:** شهري

#### سجلات الحضور:

**داخل النطاق (1-10):**
- ✅ يوم 1: حضر (بدون تأخير)
- ✅ يوم 3: حضر (بدون تأخير)
- ✅ يوم 5: حضر (تأخر 30 دقيقة)
- ✅ يوم 7: حضر (بدون تأخير)
- ✅ يوم 9: حضر (بدون تأخير)

**خارج النطاق (يجب تجاهلها):**
- ⚠️ يوم 12: حضر ← **تم تجاهله**
- ⚠️ يوم 15: حضر ← **تم تجاهله**

**النتيجة:**
```
أيام الحضور الفعلية: 5 أيام (فقط الأيام داخل النطاق)
أيام الغياب: ~3 أيام (8 متوقع - 5 حضور)
أيام تم تجاهلها: 2 يوم (أيام 12 و 15)
```

#### المكافآت:

**داخل النطاق:**
- ✅ يوم 3: مكافأة اختبار 200 جنيه ← **تم تطبيقها**

**خارج النطاق:**
- ⚠️ يوم 17: مكافأة اختبار 300 جنيه ← **تم تجاهلها**

**النتيجة المتوقعة:**
```
المستحقات:
- الراتب الأساسي: 6,000
- البدلات: 600
- المكافآت: +200
- الإجمالي: 6,800

الخصومات:
- خصم الغياب: ~692 (3 أيام × 230.77 راتب يومي)
- خصم التأخير: ~28.85 (30 دقيقة)
- التأمين (10%): 600
- الضرائب (5%): 300
- الإجمالي: ~1,620.85

صافي الراتب: ~5,179.15
```

---

## 🎯 نقاط التحقق الأساسية

### ✅ 1. الأيام خارج النطاق لا تُحتسب غياب

**السيناريو:**
- موظف 1 حضر يوم 15 (خارج النطاق 1-10)
- موظف 2 حضر يومي 12 و 15 (خارج النطاق)

**التحقق:**
```
❌ خطأ: احتساب الغياب = 26 - 3 = 23 يوم
✅ صحيح: احتساب الغياب = 8 - 3 = 5 أيام
```

**النتيجة:** ✅ الأيام خارج النطاق لا تُحسب في الغياب

---

### ✅ 2. الجزاءات خارج النطاق يتم تجاهلها

**السيناريو:**
- موظف 1 لديه جزاء 100 جنيه في يوم 3 (داخل النطاق)
- موظف 1 لديه جزاء 200 جنيه في يوم 13 (خارج النطاق)

**التحقق:**
```
❌ خطأ: إجمالي الجزاءات = 100 + 200 = 300 جنيه
✅ صحيح: إجمالي الجزاءات = 100 جنيه فقط
```

**النتيجة:** ✅ الجزاءات خارج النطاق لا تُطبق

---

### ✅ 3. المكافآت خارج النطاق يتم تجاهلها

**السيناريو:**
- موظف 2 لديه مكافأة 200 جنيه في يوم 3 (داخل النطاق)
- موظف 2 لديه مكافأة 300 جنيه في يوم 17 (خارج النطاق)

**التحقق:**
```
❌ خطأ: إجمالي المكافآت = 200 + 300 = 500 جنيه
✅ صحيح: إجمالي المكافآت = 200 جنيه فقط
```

**النتيجة:** ✅ المكافآت خارج النطاق لا تُطبق

---

### ✅ 4. عنوان كشف الرواتب يعرض النطاق

**التحقق:**
```
❌ خطأ: "كشف الرواتب - 1/2026"
✅ صحيح: "كشف الرواتب - 1/2026 (من 1 إلى 10)"
```

**النتيجة:** ✅ العنوان يعرض النطاق بوضوح

---

### ✅ 5. كارت الموظف يعرض النطاق

**التحقق:**
```
✅ اسم الموظف
✅ كود الموظف
✅ النطاق: من 1 إلى 10
```

**النتيجة:** ✅ كل كارت يعرض النطاق المحدد

---

## 📋 التقرير المُولَّد

عند تشغيل الاختبار، يتم طباعة تقرير مفصل في Console المتصفح وفي الواجهة:

```
╔════════════════════════════════════════════════════════════════╗
║           اختبار تلقائي لنظام المرتبات (Simulation)           ║
╚════════════════════════════════════════════════════════════════╝

📅 الفترة المحددة: 1/2026 (من يوم 1 إلى يوم 10)

════════════════════════════════════════════════════════════════
موظف 1: أحمد محمد (اختبار) (SIM001)
════════════════════════════════════════════════════════════════

💰 الراتب الأساسي: 5000.00
💵 البدلات: 500.00

📊 أيام العمل المتوقعة (داخل النطاق): 8 يوم

✅ أيام الحضور داخل النطاق: 3 يوم
   • 2026-01-02
   • 2026-01-05 (تأخر 15 دقيقة)
   • 2026-01-08

⚠️  أيام حضور خارج النطاق (يجب تجاهلها): 1 يوم
   ✖ 2026-01-15 ← تم تجاهله (خارج النطاق)

❌ أيام الغياب (داخل النطاق فقط): 5 يوم
   الحساب: 8 (متوقع) - 3 (حضور) = 5

⛔ الجزاءات داخل النطاق:
   • جزاء اختبار داخل النطاق: 100.00

⚠️  الجزاءات خارج النطاق (يجب تجاهلها):
   ✖ جزاء اختبار خارج النطاق (يجب تجاهله): 200.00 ← تم تجاهله

🎁 المكافآت داخل النطاق:
   • مكافأة اختبار داخل النطاق: 150.00

💵 النتيجة النهائية:
   الراتب الأساسي: 5000.00
   البدلات: 500.00
   إجمالي المستحقات: 5500.00

   خصم الغياب: -961.54
   خصم التأخير: -14.42
   الجزاءات: -100.00
   التأمين: -500.00
   الضرائب: -250.00
   المكافآت: +150.00

   إجمالي الخصومات: 1826.42
   🟢 صافي الراتب: 3823.58

════════════════════════════════════════════════════════════════
✅ نقاط التحقق الأساسية:
   1. الأيام خارج النطاق لم تُحتسب كغياب ✓
   2. الجزاءات خارج النطاق تم تجاهلها ✓
   3. المكافآت خارج النطاق تم تجاهلها ✓
   4. الحساب دقيق ضمن النطاق المحدد فقط ✓
════════════════════════════════════════════════════════════════
```

---

## 💻 الملفات المُضافة

### 1. `src/utils/payrollSimulation.ts`

**الوظائف:**

#### `createSimulationData()`
```typescript
function createSimulationData(
  year: number,
  month: number,
  fromDay: number,
  toDay: number
): {
  employees: SimulationEmployee[];
  simulationResults: SimulationResult[];
  rangeInfo: {...};
}
```

**المسؤولية:**
- إنشاء موظفين تجريبيين (2 موظف)
- إنشاء سجلات حضور داخل وخارج النطاق
- إنشاء جزاءات ومكافآت داخل وخارج النطاق
- حساب الرواتب باستخدام `calculatePayroll()` الفعلي
- إرجاع النتائج مع التفاصيل الكاملة

#### `generateSimulationReport()`
```typescript
function generateSimulationReport(
  simulationResults: SimulationResult[],
  rangeInfo: any
): string
```

**المسؤولية:**
- تنسيق التقرير بشكل واضح ومنظم
- عرض جميع البيانات والحسابات
- تمييز البيانات داخل/خارج النطاق
- إظهار نقاط التحقق الأساسية

---

### 2. تعديلات في `src/pages/Payroll.tsx`

**State جديد:**
```typescript
const [simulationMode, setSimulationMode] = useState(false);
const [simulationReport, setSimulationReport] = useState<string>('');
```

**Function جديدة:**
```typescript
function runSimulation() {
  // تشغيل الاختبار
  // إنشاء البيانات التجريبية
  // عرض التقرير
  // تحديث payrollRuns
  // التبديل إلى view الكشوفات
}
```

**useEffect جديد:**
```typescript
useEffect(() => {
  if (currentPage === 'payroll' && import.meta.env.DEV) {
    const hasRunSimulation = sessionStorage.getItem('payroll_simulation_run');
    if (!hasRunSimulation) {
      runSimulation();
      sessionStorage.setItem('payroll_simulation_run', 'true');
    }
  }
}, [currentPage]);
```

**Banner UI جديد:**
- لوحة برتقالية في أعلى الصفحة
- عرض التقرير الكامل
- زر إغلاق للعودة للبيانات الحقيقية

---

## ⚙️ كيفية عمل الاختبار

### 1. التشغيل التلقائي

```javascript
// عند فتح صفحة الرواتب في dev mode
if (import.meta.env.DEV && !hasRunSimulation) {
  runSimulation();
}
```

### 2. إنشاء البيانات

```javascript
const { simulationResults, rangeInfo } = createSimulationData(
  2026,  // year
  1,     // month (يناير)
  1,     // fromDay
  10     // toDay
);
```

### 3. الحساب الفعلي

```javascript
// يستخدم نفس دالة calculatePayroll الحقيقية
const calculation = calculatePayroll(
  employee,
  attendanceInRange,  // فقط الحضور داخل النطاق
  penaltiesInRange,   // فقط الجزاءات داخل النطاق
  [],
  workdaysPerMonth,
  0,
  bonusesInRange,     // فقط المكافآت داخل النطاق
  insuranceSettings,
  taxSettings,
  expectedWorkDaysInRange  // أيام العمل المتوقعة ضمن النطاق فقط
);
```

### 4. عرض النتائج

```javascript
// تحويل النتائج إلى payroll runs
const simulatedPayrollRuns = simulationResults.map(...)

// عرضها في UI
setPayrollRuns(simulatedPayrollRuns);
setActiveTab('payslips');
```

---

## 🔒 الأمان

### لا يعمل في Production

```javascript
if (import.meta.env.DEV) {
  // Simulation code
}
```

**التحقق:**
```javascript
import.meta.env.DEV === true   // في dev mode
import.meta.env.DEV === false  // في production build
```

### لا يتم حفظ البيانات

- جميع البيانات في الذاكرة (in-memory)
- لا يتم عمل أي `INSERT` أو `UPDATE` في قاعدة البيانات
- عند إغلاق الاختبار، تختفي البيانات تماماً

### SessionStorage للتحكم

```javascript
sessionStorage.setItem('payroll_simulation_run', 'true');
```

- يمنع إعادة التشغيل التلقائي عند التنقل
- يُحذف عند إغلاق التبويب
- يُحذف عند الضغط على زر الإغلاق

---

## 🎨 الواجهة

### Banner الاختبار

**الألوان:**
- خلفية: gradinet من أصفر إلى برتقالي
- حدود: برتقالي داكن (2px)
- نص: برتقالي داكن/أسود

**المحتوى:**
- أيقونة Flask (🧪)
- عنوان "وضع الاختبار التلقائي"
- شرح مختصر
- التقرير الكامل (scrollable)
- زر إغلاق برتقالي

### كشف الرواتب

**يعرض:**
- عنوان: "كشف الرواتب - 1/2026 (من 1 إلى 10)"
- 2 موظف تجريبي
- كل كارت يعرض:
  - اسم الموظف + "(اختبار)"
  - كود الموظف (SIM001, SIM002)
  - النطاق: من 1 إلى 10
  - أيام الحضور
  - صافي الراتب
  - جميع التفاصيل

---

## 🧪 اختبار الاختبار!

### الخطوات:

1. **تشغيل dev server:**
   ```bash
   npm run dev
   ```

2. **فتح صفحة الرواتب:**
   - انتقل إلى "إدارة الرواتب والجزاءات"

3. **مراقبة Console:**
   ```
   🧪 تشغيل الاختبار التلقائي لنظام المرتبات...
   📍 الهدف: التحقق من أن الأيام خارج النطاق لا تُحتسب كغياب
   ```

4. **التحقق من Banner:**
   - يجب ظهور لوحة برتقالية في الأعلى
   - يجب عرض التقرير الكامل

5. **التحقق من Payslips:**
   - يجب عرض 2 موظف تجريبي
   - العنوان: "كشف الرواتب - 1/2026 (من 1 إلى 10)"
   - كل كارت يعرض "النطاق: من 1 إلى 10"

6. **التحقق من الحسابات:**
   - موظف 1: 3 أيام حضور، ~5 أيام غياب
   - موظف 2: 5 أيام حضور، ~3 أيام غياب
   - الأيام خارج النطاق لم تُحتسب

7. **اختبار الإغلاق:**
   - اضغط "إغلاق الاختبار..."
   - يجب اختفاء Banner
   - يجب تفريغ payrollRuns

8. **اختبار إعادة التشغيل:**
   - أعد تحميل الصفحة (F5)
   - يجب تشغيل الاختبار من جديد

---

## 📊 النتائج المتوقعة

### موظف 1 (أحمد محمد):

```
✅ أيام الحضور: 3 (يوم 2، 5، 8)
❌ أيام الغياب: 5 (من أصل 8 أيام متوقعة)
⚠️  تم تجاهل: يوم 15 (حضور خارج النطاق)
⚠️  تم تجاهل: جزاء 200 جنيه (خارج النطاق)
✅ تم تطبيق: جزاء 100 جنيه (داخل النطاق)
✅ تم تطبيق: مكافأة 150 جنيه (داخل النطاق)

صافي الراتب: ~3,823.58 جنيه
```

### موظف 2 (فاطمة علي):

```
✅ أيام الحضور: 5 (يوم 1، 3، 5، 7، 9)
❌ أيام الغياب: 3 (من أصل 8 أيام متوقعة)
⚠️  تم تجاهل: يوم 12 و 15 (حضور خارج النطاق)
⚠️  تم تجاهل: مكافأة 300 جنيه (خارج النطاق)
✅ تم تطبيق: مكافأة 200 جنيه (داخل النطاق)

صافي الراتب: ~5,179.15 جنيه
```

---

## ✅ ملخص الميزات

| الميزة | الحالة |
|--------|---------|
| تشغيل تلقائي في dev mode | ✅ |
| بيانات تجريبية (2 موظف) | ✅ |
| حضور داخل وخارج النطاق | ✅ |
| جزاءات داخل وخارج النطاق | ✅ |
| مكافآت داخل وخارج النطاق | ✅ |
| تقرير مفصل في Console | ✅ |
| عرض تقرير في الواجهة | ✅ |
| عنوان يعرض النطاق | ✅ |
| كروت تعرض النطاق | ✅ |
| زر إغلاق الاختبار | ✅ |
| عدم التأثير على Production | ✅ |
| عدم الحفظ في قاعدة البيانات | ✅ |

---

## 🎯 الخلاصة

الاختبار التلقائي يوفر:

1. ✅ **تحقق فوري**: من دقة حساب النطاق الزمني
2. ✅ **بيانات واضحة**: سيناريوهات محددة ومعروفة النتائج
3. ✅ **سهولة الاستخدام**: يعمل تلقائياً بدون تدخل
4. ✅ **أمان كامل**: لا يؤثر على البيانات الحقيقية
5. ✅ **توثيق حي**: يعرض كيفية عمل النظام

**جاهز للاستخدام الفوري!** 🚀
